# 2026-07-27

## 项目重命名：pico → ellie

用户买来的项目，为简历防撞需要改名。选定新名字为 **Ellie**（艾莉）。

**排除范围**：`docs/review-pack/interview-qa.md`（面试手册）和 `benchmarks/` 不修改。

**已完成**：
- 目录重命名：`pico/` → `ellie/`，`examples/mini-pico/` → `examples/mini-ellie/`，`mini_pico/` → `mini_ellie/`
- 删除了 `pico.egg-info/`
- Claude Code 执行了全部源码替换：tests 125 passed, 9 failed（均为 Windows 预存问题）
- Claude Code 执行提示词存档：`ELLIE-REFACTOR-PROMPT.md`

## Phase 2：个性化

- 替换了欢迎 ASCII 画（猫 → 小人脸）和状态文字（`calm shell, ready for work` → `ready.`）
- 版本号 `0.1.0` → `1.0.0`（含 mini-ellie）
- 新增 `git_diff` 工具：只读工具，查看 git 工作区变更，这是原项目没有的差异化功能
- 修复 `.env` 和 `.env.example` 中环境变量前缀（PICO_ → ELLIE_），否则启动报 401
- 重写 README：因编码损坏，从头重写为个人风格，新增工具列表表格

## Phase 3：AI 应用能力增强计划

- 分析项目对秋招 AI 应用开发岗的匹配度：架构强（agent loop/安全/多模型）、但缺 RAG、流式、HTTP 服务、外部集成
- 生成四阶段 Claude Code 执行提示词：`ELLIE-AI-APP-PROMPT.md`
  - A: RAG 知识检索（retrieve 工具 + ingest 命令，TF-IDF + 余弦相似度，零依赖）
  - B: web_fetch 工具（urllib 抓取 URL）
  - C: 流式输出（所有 client 加 complete_stream()，CLI --stream 参数）
  - D: HTTP API 服务（FastAPI server，/ask /sessions /reset /health）
